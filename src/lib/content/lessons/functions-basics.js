/** @type {import('../schema').Lesson} */
export default {
	"slug": "functions-basics",
	"title": "Свои функции: учимся не повторяться",
	"level": "intermediate",
	"topic": "python",
	"order": 12,
	"estimatedMinutes": 60,
	"emoji": "🧩",
	"summary": "Функции помогают давать имя куску кода и переиспользовать его. Учимся объявлять функции через def, передавать аргументы и возвращать результат через return.",
	"goals": [
		"Объявлять свою функцию через def",
		"Передавать данные через параметры и аргументы",
		"Возвращать результат через return",
		"Понимать разницу между print() и return",
		"Разбивать задачу на маленькие функции"
	],
	"theory": [
		{
			"type": "text",
			"md": "Представь, что в коде тебе раз за разом нужно делать одно и то же: поздороваться с пользователем, посчитать площадь, перевести цену с налогом. Копировать одни и те же строки снова и снова - скучно и опасно: где-то опечатаешься, где-то забудешь поправить. На помощь приходят **функции**.\n\n**Функция** - это маленькая программа внутри программы. Ты описываешь её один раз, даёшь ей имя, а потом просто зовёшь по имени столько раз, сколько нужно. Это и есть главный принцип: **не повторяйся**."
		},
		{
			"type": "text",
			"md": "Функция объявляется с помощью ключевого слова `def` (от английского *define* - определить). Схема такая:\n\n```\ndef имя_функции(параметры):\n    тело функции\n```\n\nОбрати внимание на детали:\n- после имени идут круглые скобки\n- в конце строки обязательно двоеточие `:`\n- тело функции пишется с отступом (обычно 4 пробела)\n\nСамо объявление функцию НЕ запускает. Это как записать рецепт: блюдо ещё не готово, пока ты не начнёшь готовить."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "def greet(name):\n    print(\"Привет, \" + name + \"!\")\n\ngreet(\"Аня\")\ngreet(\"Борис\")",
			"runnable": true,
			"output": "Привет, Аня!\nПривет, Борис!"
		},
		{
			"type": "text",
			"md": "Разберём пример выше:\n- `name` в скобках при объявлении - это **параметр**. Это как пустая коробочка с подписью, в которую при вызове положат значение.\n- Когда мы пишем `greet(\"Аня\")`, строка `\"Аня\"` - это **аргумент**. Это конкретное значение, которое мы передаём в функцию.\n\nПростыми словами: **параметр** - это имя в определении функции, а **аргумент** - реальное значение, которое мы подставляем при вызове. Один раз написали функцию - и зовём её с разными аргументами."
		},
		{
			"type": "callout",
			"variant": "info",
			"title": "Параметр или аргумент?",
			"md": "Не переживай, если путаешь эти слова - так делают почти все. Запомни по-простому: **параметр** живёт в строке с `def`, а **аргумент** живёт в круглых скобках при вызове функции. На практике разницу часто не подчёркивают."
		},
		{
			"type": "text",
			"md": "Функция может не только что-то печатать, но и **возвращать результат** - чтобы мы могли его сохранить и использовать дальше. Для этого есть ключевое слово `return`.\n\nКогда Python доходит до `return`, он немедленно выходит из функции и отдаёт указанное значение туда, откуда функцию позвали."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "def square(x):\n    return x * x\n\nresult = square(5)\nprint(result)\nprint(square(3) + square(4))",
			"runnable": true,
			"output": "25\n25"
		},
		{
			"type": "text",
			"md": "Смотри, что произошло:\n- `square(5)` вернул число `25`, и мы сохранили его в переменную `result`.\n- А `square(3) + square(4)` - это `9 + 16`, то есть тоже `25`. Раз функция возвращает значение, его можно складывать, сравнивать, передавать дальше - как обычное число."
		},
		{
			"type": "callout",
			"variant": "warning",
			"title": "Самое частое недоразумение новичка: print vs return",
			"md": "`print()` просто **показывает** текст на экране - человеку. `return` **отдаёт** значение обратно в программу, чтобы код мог его использовать.\n\nЕсли функция только печатает и ничего не возвращает, то она возвращает специальное пустое значение `None`. Сохранить такой результат в переменную можно, но толку от него не будет."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "def show():\n    print(\"я печатаю\")\n\nx = show()\nprint(x)",
			"runnable": true,
			"output": "я печатаю\nNone"
		},
		{
			"type": "text",
			"md": "Видишь? Функция `show()` напечатала свою строку, но вернула `None`, потому что в ней нет `return`. Поэтому `print(x)` показал `None`. Правило простое: **хочешь использовать результат дальше в коде - используй `return`. Хочешь просто показать что-то пользователю - используй `print()`.**"
		},
		{
			"type": "text",
			"md": "У функции может быть несколько параметров - перечисляй их через запятую. И возвращать она может результат вычислений, а не просто готовую переменную."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "def price_with_tax(price, tax):\n    return price + price * tax\n\ntotal = price_with_tax(100, 0.2)\nprint(total)",
			"runnable": true,
			"output": "120.0"
		},
		{
			"type": "callout",
			"variant": "tip",
			"title": "Разбивай задачу на функции",
			"md": "Большую задачу удобно резать на маленькие функции, каждая из которых делает что-то одно. Потом эти функции можно соединять: результат одной передавать в другую. Это называется **композиция**. Маленькие функции легче понимать, проверять и переиспользовать - и ты перестаёшь повторять один и тот же код."
		},
		{
			"type": "text",
			"md": "Краткий итог урока:\n- `def` объявляет функцию, тело пишется с отступом.\n- **Параметры** - имена в скобках при объявлении, **аргументы** - значения при вызове.\n- `return` отдаёт результат обратно в программу; без него функция возвращает `None`.\n- `print()` показывает текст человеку, `return` передаёт значение коду - это разные вещи.\n- Дели задачу на маленькие функции и собирай из них решение. Так ты не повторяешься."
		}
	],
	"exercises": [
		{
			"id": "rectangle-area",
			"lang": "python",
			"prompt": "Напиши функцию `rectangle_area(width, height)`, которая принимает ширину и высоту прямоугольника и **возвращает** его площадь (ширина умножить на высоту).\n\nВажно: функция должна именно `return` возвращать число, а не печатать его через `print()`.",
			"starter": "def rectangle_area(width, height):\n    # верни площадь: ширина умножить на высоту\n    # используй return\n    pass\n",
			"hints": [
				"Площадь прямоугольника - это `width * height`.",
				"Не печатай результат через `print()`, а отдавай его через `return`.",
				"Готовая строка тела функции: `return width * height`."
			],
			"solution": "def rectangle_area(width, height):\n    return width * height\n",
			"tests": [
				{
					"name": "rectangle_area(2, 3) == 6",
					"code": "assert rectangle_area(2, 3) == 6, \"Площадь 2 на 3 должна быть 6\""
				},
				{
					"name": "rectangle_area(5, 5) == 25",
					"code": "assert rectangle_area(5, 5) == 25, \"Площадь 5 на 5 должна быть 25\""
				},
				{
					"name": "rectangle_area(10, 1) == 10",
					"code": "assert rectangle_area(10, 1) == 10, \"Площадь 10 на 1 должна быть 10\""
				},
				{
					"name": "rectangle_area(0, 7) == 0",
					"code": "assert rectangle_area(0, 7) == 0, \"Площадь 0 на 7 должна быть 0\""
				}
			]
		},
		{
			"id": "bigger-of-two",
			"lang": "python",
			"prompt": "Напиши функцию `bigger(a, b)`, которая принимает два числа и **возвращает** большее из них. Если числа равны, можно вернуть любое из них (они одинаковые).\n\nПодсказка: тебе пригодится условие `if`.",
			"starter": "def bigger(a, b):\n    # сравни a и b\n    # верни то, что больше\n    pass\n",
			"hints": [
				"Сравни числа с помощью `if a > b:`.",
				"В ветке `if` верни `a`, иначе верни `b`.",
				"Можно написать короче: `return a if a >= b else b`."
			],
			"solution": "def bigger(a, b):\n    if a >= b:\n        return a\n    return b\n",
			"tests": [
				{
					"name": "bigger(3, 5) == 5",
					"code": "assert bigger(3, 5) == 5, \"Большее из 3 и 5 это 5\""
				},
				{
					"name": "bigger(10, 2) == 10",
					"code": "assert bigger(10, 2) == 10, \"Большее из 10 и 2 это 10\""
				},
				{
					"name": "bigger(4, 4) == 4",
					"code": "assert bigger(4, 4) == 4, \"Если числа равны, вернуть 4\""
				},
				{
					"name": "bigger(-1, -5) == -1",
					"code": "assert bigger(-1, -5) == -1, \"Большее из -1 и -5 это -1\""
				}
			]
		},
		{
			"id": "compose-functions",
			"lang": "python",
			"prompt": "Потренируемся в **композиции** - когда одна функция использует результат другой.\n\nНапиши три функции:\n- `double(x)` - возвращает `x`, умноженное на 2.\n- `add_one(x)` - возвращает `x` плюс 1.\n- `double_then_add_one(x)` - сначала удваивает число, потом прибавляет 1. Внутри обязательно **вызови** уже написанные `double` и `add_one`, а не считай заново.\n\nНапример, для числа 3: сначала удвоили - получили 6, потом прибавили 1 - получили 7.",
			"starter": "def double(x):\n    # верни x умножить на 2\n    pass\n\ndef add_one(x):\n    # верни x плюс 1\n    pass\n\ndef double_then_add_one(x):\n    # используй double и add_one\n    pass\n",
			"hints": [
				"`double` это `return x * 2`, а `add_one` это `return x + 1`.",
				"В `double_then_add_one` сначала получи удвоенное значение, позвав `double(x)`.",
				"Соедини вызовы: `return add_one(double(x))`."
			],
			"solution": "def double(x):\n    return x * 2\n\ndef add_one(x):\n    return x + 1\n\ndef double_then_add_one(x):\n    return add_one(double(x))\n",
			"tests": [
				{
					"name": "double(4) == 8",
					"code": "assert double(4) == 8, \"double(4) должно быть 8\""
				},
				{
					"name": "add_one(9) == 10",
					"code": "assert add_one(9) == 10, \"add_one(9) должно быть 10\""
				},
				{
					"name": "double_then_add_one(3) == 7",
					"code": "assert double_then_add_one(3) == 7, \"double_then_add_one(3) должно быть 7\""
				},
				{
					"name": "double_then_add_one(0) == 1",
					"code": "assert double_then_add_one(0) == 1, \"double_then_add_one(0) должно быть 1\""
				},
				{
					"name": "double_then_add_one(10) == 21",
					"code": "assert double_then_add_one(10) == 21, \"double_then_add_one(10) должно быть 21\""
				}
			]
		}
	],
	"quiz": [
		{
			"question": "В чём разница между параметром и аргументом?",
			"options": [
				"Это одно и то же, просто разные слова без всякой разницы",
				"Параметр - это имя в скобках при объявлении функции, а аргумент - конкретное значение, которое передают при вызове",
				"Параметр передают при вызове, а аргумент пишут в строке с def",
				"Аргумент - это значение, которое функция возвращает через return"
			],
			"correct": 1,
			"explain": "Параметр живёт в определении функции (в строке с `def`), а аргумент - это реальное значение, которое мы подставляем при вызове, например `greet(\"Аня\")`."
		},
		{
			"question": "Что вернёт функция, если внутри неё нет ни одного return?",
			"options": [
				"Ноль (0)",
				"Пустую строку",
				"Специальное значение None",
				"Ошибку при вызове"
			],
			"correct": 2,
			"explain": "Если в функции нет `return`, она всё равно завершается и возвращает `None` - специальное пустое значение. Поэтому, чтобы получить полезный результат, нужно явно писать `return`."
		},
		{
			"question": "Чем отличается print() от return внутри функции?",
			"options": [
				"Ничем, оба показывают результат на экране",
				"print() показывает текст человеку на экране, а return отдаёт значение обратно в программу для дальнейшего использования",
				"return печатает на экран, а print() сохраняет значение в переменную",
				"print() работает только с числами, а return только с текстом"
			],
			"correct": 1,
			"explain": "`print()` нужен, чтобы показать что-то пользователю на экране. `return` отдаёт значение обратно в код, и его можно сохранить в переменную или использовать в вычислениях. Это разные задачи."
		}
	]
};
