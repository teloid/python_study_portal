/** @type {import('../schema').Lesson} */
export default {
	"slug": "loops-for-while",
	"title": "Повторяем без скуки: циклы for и while",
	"level": "beginner",
	"topic": "python",
	"order": 7,
	"estimatedMinutes": 55,
	"emoji": "🔁",
	"summary": "Заставляем компьютер делать однообразную работу за нас. Изучаем цикл for с range(), цикл while и управление циклом через break и continue.",
	"goals": [
		"Повторять действия циклом for и функцией range()",
		"Использовать цикл while и его условие остановки",
		"Управлять циклом через break и continue",
		"Считать сумму и количество в цикле",
		"Избегать бесконечного цикла"
	],
	"theory": [
		{
			"type": "text",
			"md": "Привет! Сегодня разберём **циклы** - это способ повторять действия много раз, не переписывая код вручную. Представь, что нужно поздороваться с 100 людьми. Писать `print` сто раз? Скучно! Цикл сделает это за нас."
		},
		{
			"type": "text",
			"md": "В Python есть два главных цикла:\n- `for` - когда заранее знаем, **сколько** раз повторять (или перебираем готовый список).\n- `while` - когда повторяем, **пока** выполняется условие."
		},
		{
			"type": "text",
			"md": "**Цикл `for` и функция `range`.** Чаще всего `for` ходит по числам, которые даёт `range`. Запись `range(5)` даёт числа от 0 до 4 (пять штук, но НЕ включая саму 5)."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "for i in range(5):\n    print(i)",
			"runnable": true,
			"output": "0\n1\n2\n3\n4"
		},
		{
			"type": "callout",
			"variant": "info",
			"title": "Как читать range",
			"md": "- `range(5)` - числа 0, 1, 2, 3, 4\n- `range(1, 6)` - числа 1, 2, 3, 4, 5 (начало включается, конец нет)\n- `range(0, 10, 2)` - с шагом 2: 0, 2, 4, 6, 8\n\nЗапомни главное: **правая граница не входит** в результат."
		},
		{
			"type": "text",
			"md": "**Накопление суммы.** Очень частый приём: завести переменную-копилку (например `total`), и на каждом шаге что-то к ней прибавлять. Посчитаем сумму чисел от 1 до 5:"
		},
		{
			"type": "code",
			"lang": "python",
			"code": "total = 0\nfor i in range(1, 6):\n    total += i\nprint(total)",
			"runnable": true,
			"output": "15"
		},
		{
			"type": "callout",
			"variant": "tip",
			"title": "Что такое +=",
			"md": "Запись `total += i` - это короткая форма для `total = total + i`. То есть \"увеличь `total` на `i`\". Точно так же есть `-=`, `*=` и другие. Для счётчика часто пишут `count += 1`."
		},
		{
			"type": "text",
			"md": "**Перебор списка.** Цикл `for` умеет ходить не только по числам, но и по элементам списка. Ключевое слово `in` означает \"для каждого элемента из\":"
		},
		{
			"type": "code",
			"lang": "python",
			"code": "fruits = [\"яблоко\", \"груша\", \"слива\"]\nfor f in fruits:\n    print(f)",
			"runnable": true,
			"output": "яблоко\nгруша\nслива"
		},
		{
			"type": "text",
			"md": "**Цикл `while`.** Он работает иначе: повторяет тело, **пока** условие истинно. Важно, чтобы внутри цикла что-то менялось, иначе он никогда не остановится. Здесь удваиваем число, пока оно не больше 16:"
		},
		{
			"type": "code",
			"lang": "python",
			"code": "n = 1\nwhile n <= 16:\n    print(n)\n    n = n * 2",
			"runnable": true,
			"output": "1\n2\n4\n8\n16"
		},
		{
			"type": "callout",
			"variant": "warning",
			"title": "Берегись бесконечного цикла",
			"md": "Если в цикле `while` условие никогда не станет ложным (например, забыли менять переменную), программа зависнет навсегда. Всегда проверяй, что внутри цикла есть шаг, который приближает к остановке."
		},
		{
			"type": "text",
			"md": "**Управление циклом: `break` и `continue`.**\n- `break` - немедленно **выходит** из цикла.\n- `continue` - **пропускает** остаток текущего шага и переходит к следующему."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "for i in range(1, 8):\n    if i == 4:\n        break\n    print(i)",
			"runnable": true,
			"output": "1\n2\n3"
		},
		{
			"type": "code",
			"lang": "python",
			"code": "for i in range(1, 6):\n    if i % 2 == 0:\n        continue\n    print(i)",
			"runnable": true,
			"output": "1\n3\n5"
		},
		{
			"type": "callout",
			"variant": "tip",
			"title": "Когда какой цикл брать",
			"md": "- Знаешь число повторений или перебираешь список - бери `for`.\n- Повторяешь до какого-то события (\"пока не\", \"до тех пор пока\") - бери `while`.\n\nА `break` и `continue` работают одинаково в обоих циклах."
		}
	],
	"exercises": [
		{
			"id": "sum-to-n",
			"lang": "python",
			"prompt": "Напиши функцию `sum_to_n(n)`, которая возвращает **сумму всех целых чисел от 1 до n включительно**.\n\nНапример, `sum_to_n(5)` равно `1 + 2 + 3 + 4 + 5 = 15`.\n\nЕсли `n` равно 0, сумма равна 0 (складывать нечего).",
			"starter": "def sum_to_n(n):\n    # заведи переменную-копилку, например total = 0\n    # пройди циклом for по числам от 1 до n включительно (подумай про range)\n    # на каждом шаге прибавляй число к копилке\n    # верни результат\n    pass",
			"hints": [
				"Используй `range(1, n + 1)`, чтобы получить числа от 1 до n включительно (правая граница не входит, поэтому пишем n + 1).",
				"Перед циклом создай переменную `total = 0`, а внутри цикла пиши `total += i`.",
				"После цикла не забудь `return total`. При n = 0 цикл просто не выполнится, и вернётся 0 - это правильно."
			],
			"solution": "def sum_to_n(n):\n    total = 0\n    for i in range(1, n + 1):\n        total += i\n    return total",
			"tests": [
				{
					"name": "sum_to_n(5) == 15",
					"code": "assert sum_to_n(5) == 15, \"sum_to_n(5) должно быть 15\""
				},
				{
					"name": "sum_to_n(1) == 1",
					"code": "assert sum_to_n(1) == 1, \"sum_to_n(1) должно быть 1\""
				},
				{
					"name": "sum_to_n(10) == 55",
					"code": "assert sum_to_n(10) == 55, \"sum_to_n(10) должно быть 55\""
				},
				{
					"name": "sum_to_n(0) == 0",
					"code": "assert sum_to_n(0) == 0, \"sum_to_n(0) должно быть 0\""
				},
				{
					"name": "sum_to_n(100) == 5050",
					"code": "assert sum_to_n(100) == 5050, \"sum_to_n(100) должно быть 5050\""
				}
			]
		},
		{
			"id": "count-even",
			"lang": "python",
			"prompt": "Напиши функцию `count_even(numbers)`, которая принимает список чисел и возвращает, **сколько в нём чётных чисел**.\n\nЧисло чётное, если делится на 2 без остатка. Проверить остаток помогает оператор `%`: выражение `x % 2 == 0` истинно для чётных чисел.\n\nНапример, `count_even([1, 2, 3, 4, 5, 6])` равно `3` (это 2, 4 и 6).",
			"starter": "def count_even(numbers):\n    # заведи счётчик count = 0\n    # пройди циклом for по элементам списка (используй in)\n    # если элемент чётный (x % 2 == 0), увеличь счётчик на 1\n    # верни счётчик\n    pass",
			"hints": [
				"Перебирай элементы напрямую: `for x in numbers:` - так `x` по очереди станет каждым числом из списка.",
				"Внутри цикла проверяй условие `if x % 2 == 0:` и только тогда делай `count += 1`.",
				"Для пустого списка цикл не выполнится ни разу, и функция вернёт 0 - это верный ответ. Не забудь `return count` в конце."
			],
			"solution": "def count_even(numbers):\n    count = 0\n    for x in numbers:\n        if x % 2 == 0:\n            count += 1\n    return count",
			"tests": [
				{
					"name": "count_even([1,2,3,4,5,6]) == 3",
					"code": "assert count_even([1, 2, 3, 4, 5, 6]) == 3, \"ожидалось 3\""
				},
				{
					"name": "count_even([1,3,5]) == 0",
					"code": "assert count_even([1, 3, 5]) == 0, \"нет чётных -> 0\""
				},
				{
					"name": "count_even([]) == 0",
					"code": "assert count_even([]) == 0, \"пустой список -> 0\""
				},
				{
					"name": "count_even([2,4,8,10]) == 4",
					"code": "assert count_even([2, 4, 8, 10]) == 4, \"ожидалось 4\""
				},
				{
					"name": "count_even([0,-2,-3]) == 2",
					"code": "assert count_even([0, -2, -3]) == 2, \"0 и -2 чётные -> 2\""
				}
			]
		},
		{
			"id": "first-multiples",
			"lang": "python",
			"prompt": "Напиши функцию `first_multiples(k, n)`, которая возвращает **список из первых n чисел, кратных k**.\n\nНапример, `first_multiples(3, 4)` равно `[3, 6, 9, 12]` (первые четыре числа, кратные 3).\n\nЕсли `n` равно 0, верни пустой список `[]`.\n\nЗдесь удобно применить цикл `while`: повторяй, пока в списке меньше n элементов.",
			"starter": "def first_multiples(k, n):\n    # создай пустой список result = []\n    # заведи счётчик i = 1\n    # пока длина result меньше n (используй len и while):\n    #   добавь в список k * i (метод .append())\n    #   увеличь i на 1\n    # верни result\n    pass",
			"hints": [
				"Создай `result = []` и переменную `i = 1`. Условие цикла: `while len(result) < n:`.",
				"Внутри цикла добавляй очередное кратное: `result.append(k * i)`, а затем обязательно делай `i += 1`, иначе цикл будет бесконечным.",
				"При n = 0 условие `len(result) < n` сразу ложно, цикл не выполнится, и вернётся пустой список. В конце напиши `return result`."
			],
			"solution": "def first_multiples(k, n):\n    result = []\n    i = 1\n    while len(result) < n:\n        result.append(k * i)\n        i += 1\n    return result",
			"tests": [
				{
					"name": "first_multiples(3,4) == [3,6,9,12]",
					"code": "assert first_multiples(3, 4) == [3, 6, 9, 12], \"first_multiples(3,4)\""
				},
				{
					"name": "first_multiples(5,3) == [5,10,15]",
					"code": "assert first_multiples(5, 3) == [5, 10, 15], \"first_multiples(5,3)\""
				},
				{
					"name": "first_multiples(1,5) == [1,2,3,4,5]",
					"code": "assert first_multiples(1, 5) == [1, 2, 3, 4, 5], \"first_multiples(1,5)\""
				},
				{
					"name": "first_multiples(2,0) == []",
					"code": "assert first_multiples(2, 0) == [], \"n=0 -> пустой список\""
				},
				{
					"name": "first_multiples(10,1) == [10]",
					"code": "assert first_multiples(10, 1) == [10], \"first_multiples(10,1)\""
				}
			]
		}
	],
	"quiz": [
		{
			"question": "Какие числа напечатает цикл `for i in range(1, 4): print(i)`?",
			"options": [
				"1, 2, 3",
				"1, 2, 3, 4",
				"0, 1, 2, 3",
				"1, 2, 3, 4, 5"
			],
			"correct": 0,
			"explain": "range(1, 4) даёт числа от 1 до 4, НЕ включая правую границу 4. Значит, печатаются 1, 2, 3."
		},
		{
			"question": "Что делает `break` внутри цикла?",
			"options": [
				"Пропускает текущий шаг и переходит к следующему",
				"Немедленно выходит из цикла",
				"Перезапускает цикл с самого начала",
				"Останавливает всю программу"
			],
			"correct": 1,
			"explain": "`break` полностью прекращает цикл и переходит к коду после него. А вот `continue` пропускает только остаток текущего шага и идёт к следующему."
		},
		{
			"question": "Какой цикл лучше выбрать, если мы повторяем действие, ПОКА выполняется некое условие, и заранее не знаем число повторений?",
			"options": [
				"for",
				"while",
				"range",
				"break"
			],
			"correct": 1,
			"explain": "`while` создан именно для случая \"повторяй, пока условие истинно\". `for` удобнее, когда число повторений известно или мы перебираем готовый список."
		}
	]
};
