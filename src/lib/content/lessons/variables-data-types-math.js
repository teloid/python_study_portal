/** @type {import('../schema').Lesson} */
export default {
	slug: 'variables-data-types-math',
	title: 'Переменные, числа и немного математики',
	level: 'beginner',
	topic: 'python',
	order: 2,
	estimatedMinutes: 45,
	emoji: '🔢',
	summary:
		'Знакомимся с переменными — коробочками для хранения данных. Разбираем типы данных: целые и дробные числа, строки и логический тип. Учимся считать в Python и проверять тип через type().',
	goals: [
		'Создавать переменные и давать им понятные имена',
		'Различать типы данных: int, float, str, bool',
		'Выполнять арифметику: +, -, *, /, //, %, **',
		'Узнавать тип значения с помощью type()',
		'Преобразовывать значения через int(), float(), str()'
	],

	theory: [
		{
			type: 'text',
			md: '**Переменная** — это коробочка с именем, в которой хранится значение. Кладём значение через знак `=` (он читается «присвоить», а не «равно»):'
		},
		{
			type: 'code',
			lang: 'python',
			runnable: true,
			code: 'name = "Аня"\nage = 25\nprint(name)\nprint(age)',
			output: 'Аня\n25'
		},
		{
			type: 'text',
			md: 'Слева от `=` — имя переменной, справа — значение. Потом можно использовать имя сколько угодно раз. Имя пишите понятным: `age` лучше, чем `a`. Имена — латиницей, без пробелов (используйте `_`), и не начинаются с цифры.'
		},
		{
			type: 'text',
			md: 'У значений есть **тип**. Четыре главных типа для начала:'
		},
		{
			type: 'text',
			md: '- `int` — целое число (от *integer*): `5`, `-100`, `0`\n- `float` — дробное число (от *float*, «с плавающей точкой»): `3.14`, `-0.5`. Дробь пишется через **точку**, не запятую!\n- `str` — строка, то есть текст (от *string*): `"привет"`\n- `bool` — логический тип (от *boolean*): только `True` (истина) или `False` (ложь)'
		},
		{
			type: 'text',
			md: 'Узнать тип значения можно командой `type()`:'
		},
		{
			type: 'code',
			lang: 'python',
			runnable: true,
			code: 'print(type(5))\nprint(type(3.14))\nprint(type("привет"))\nprint(type(True))',
			output: "<class 'int'>\n<class 'float'>\n<class 'str'>\n<class 'bool'>"
		},
		{
			type: 'text',
			md: 'Python умеет считать. Основные арифметические операторы:'
		},
		{
			type: 'code',
			lang: 'python',
			runnable: true,
			code: 'print(7 + 3)    # сложение -> 10\nprint(7 - 3)    # вычитание -> 4\nprint(7 * 3)    # умножение -> 21\nprint(7 / 3)    # деление -> 2.333...\nprint(7 // 3)   # целая часть от деления -> 2\nprint(7 % 3)    # остаток от деления -> 1\nprint(2 ** 3)   # степень: 2 в кубе -> 8',
			output: '10\n4\n21\n2.3333333333333335\n2\n2\n1\n8'
		},
		{
			type: 'callout',
			variant: 'info',
			title: 'Деление / и //',
			md: 'Обычное деление `/` всегда даёт дробь (`float`): `10 / 2` это `5.0`, а не `5`. Двойной слэш `//` даёт целую часть (отбрасывает дробь): `7 // 3` это `2`. А `%` (его называют «процент», но здесь это **остаток**) — то, что осталось: `7 % 3` это `1`.'
		},
		{
			type: 'text',
			md: 'Иногда число приходит как текст (например, из ввода), и его нужно превратить в настоящее число. Для этого есть `int()` и `float()`. Обратно в текст — `str()`:'
		},
		{
			type: 'code',
			lang: 'python',
			runnable: true,
			code: 'text = "30"\nnumber = int(text)   # строка "30" -> число 30\nprint(number + 5)    # теперь можно складывать -> 35',
			output: '35'
		},
		{
			type: 'callout',
			variant: 'warning',
			title: 'Текст и число — не одно и то же',
			md: 'Если попробовать сложить число с текстом, например `5 + "3"`, Python выдаст ошибку `TypeError` — он не знает, складывать это как числа или склеивать как текст. Сначала приведите всё к одному типу через `int()` или `str()`.'
		}
	],

	exercises: [
		{
			id: 'rectangle-area',
			lang: 'python',
			prompt:
				'Посчитайте площадь прямоугольника. Создайте переменные `width = 8` и `height = 5`, затем переменную `area`, равную их произведению. Площадь выводить не нужно — мы проверим саму переменную `area`.',
			starter: 'width = 8\nheight = 5\n# создайте переменную area\n',
			hints: [
				'Площадь прямоугольника — это ширина, умноженная на высоту.',
				'Умножение в Python — это знак *',
				'area = width * height'
			],
			solution: 'width = 8\nheight = 5\narea = width * height',
			tests: [
				{ name: 'Переменная area существует и равна 40', code: 'assert area == 40, f"area должно быть 40, а получилось {area}"' }
			]
		},
		{
			id: 'time-split',
			lang: 'python',
			prompt:
				'Дано общее число минут: `total_minutes = 145`. Разложите его на часы и минуты. Создайте `hours` (сколько целых часов) и `minutes` (сколько минут осталось). Подсказка: в часе 60 минут.',
			starter: 'total_minutes = 145\n# hours = ...\n# minutes = ...\n',
			hints: [
				'Целое число часов — это целочисленное деление total_minutes на 60 (оператор //).',
				'Оставшиеся минуты — это остаток от деления на 60 (оператор %).',
				'hours = total_minutes // 60  и  minutes = total_minutes % 60'
			],
			solution: 'total_minutes = 145\nhours = total_minutes // 60\nminutes = total_minutes % 60',
			tests: [
				{ name: 'hours равно 2', code: 'assert hours == 2, f"hours должно быть 2, а получилось {hours}"' },
				{ name: 'minutes равно 25', code: 'assert minutes == 25, f"minutes должно быть 25, а получилось {minutes}"' }
			]
		},
		{
			id: 'string-to-number',
			lang: 'python',
			prompt:
				'Возраст пришёл как текст: `age_text = "30"`. Превратите его в число, прибавьте 1 (через год) и сохраните результат в переменную `age_next`. Она должна быть числом, а не текстом.',
			starter: 'age_text = "30"\n# age_next = ...\n',
			hints: [
				'Чтобы превратить строку в целое число, используйте int().',
				'int(age_text) даст число 30.',
				'age_next = int(age_text) + 1'
			],
			solution: 'age_text = "30"\nage_next = int(age_text) + 1',
			tests: [
				{ name: 'age_next равно 31', code: 'assert age_next == 31, f"ожидалось 31, получилось {age_next}"' },
				{
					name: 'age_next — это число (int), а не строка',
					code: 'assert isinstance(age_next, int), "age_next должно быть числом, используйте int()"'
				}
			]
		}
	],

	quiz: [
		{
			question: 'Какой тип у значения 3.14?',
			options: ['int', 'float', 'str', 'bool'],
			correct: 1,
			explain: 'Число с дробной частью — это float (с плавающей точкой). Дробь записывается через точку.'
		},
		{
			question: 'Сколько будет 17 % 5 (остаток от деления)?',
			options: ['3', '2', '3.4', '85'],
			correct: 1,
			explain: '17 делится на 5 три раза (15), остаётся 2. Оператор % возвращает остаток.'
		},
		{
			question: 'Что вернёт int("12")?',
			options: ['Строку "12"', 'Число 12', 'Ошибку', 'Число 12.0'],
			correct: 1,
			explain: 'int() превращает строку с числом в целое число. Получится 12 типа int.'
		}
	]
};
