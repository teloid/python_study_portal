/** @type {import('../schema').Lesson} */
export default {
	"slug": "practice-conditions-loops",
	"title": "Практикум: условия и циклы",
	"level": "beginner",
	"topic": "python",
	"order": 17,
	"estimatedMinutes": 45,
	"emoji": "🔀",
	"summary": "Смешиваем if/elif/else с циклами for и while на разных сюжетах: FizzBuzz, проверка пароля, суммы и подсчёты. Тренируем логику до автоматизма.",
	"goals": [
		"Свободно ветвить логику через if/elif/else",
		"Комбинировать условия с циклами",
		"Накапливать сумму и счётчик в цикле",
		"Разбирать задачу на шаги"
	],
	"theory": [
		{
			"type": "text",
			"md": "Привет! Сегодня без длинной теории - сразу разминаем **условия** и **циклы** на живых задачах. Быстро вспомним три инструмента, а дальше - практика."
		},
		{
			"type": "callout",
			"variant": "info",
			"title": "Шпаргалка по условиям",
			"md": "- `if` - проверяем условие; `elif` - \"а иначе если\"; `else` - \"во всех остальных случаях\".\n- Проверки идут **сверху вниз**, срабатывает первая подходящая ветка.\n- Порядок важен: если хотим \"90 и выше\", ставим эту проверку раньше, чем \"80 и выше\"."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "score = 85\nif score >= 90:\n    print(\"отлично\")\nelif score >= 70:\n    print(\"хорошо\")\nelse:\n    print(\"надо подтянуть\")",
			"runnable": true,
			"output": "хорошо"
		},
		{
			"type": "callout",
			"variant": "tip",
			"title": "Циклы: for, while и управление",
			"md": "- `for i in range(1, n + 1)` - идём по числам от 1 до `n` включительно (правая граница не входит, поэтому `n + 1`).\n- `while условие` - повторяем, **пока** условие истинно (следи, чтобы оно когда-то стало ложным!).\n- `break` - сразу выйти из цикла; `continue` - пропустить остаток шага и идти дальше.\n- Проверить чётность помогает остаток: `x % 2 == 0`. А чтобы узнать, цифра ли символ - метод `.isdigit()`."
		}
	],
	"exercises": [
		{
			"id": "fizzbuzz",
			"lang": "python",
			"prompt": "Классика собеседований - **FizzBuzz**! Напиши функцию `fizzbuzz(n)`, которая возвращает **список строк** для чисел от 1 до `n` включительно по правилам:\n\n- число делится на 3 и на 5 (то есть на 15) -> `\"FizzBuzz\"`\n- делится только на 3 -> `\"Fizz\"`\n- делится только на 5 -> `\"Buzz\"`\n- иначе -> само число, но **строкой** (например `\"7\"`).\n\nНапример, `fizzbuzz(5)` равно `[\"1\", \"2\", \"Fizz\", \"4\", \"Buzz\"]`.\n\nЕсли `n` равно 0, верни пустой список `[]`.",
			"starter": "def fizzbuzz(n):\n    # заведи пустой список result = []\n    # пройди циклом for по числам от 1 до n включительно\n    # СНАЧАЛА проверь деление на 15, потом на 3, потом на 5, иначе - само число строкой\n    # добавляй нужную строку в result через .append()\n    # верни result\n    pass",
			"hints": [
				"Порядок проверок важен! Сначала `if i % 15 == 0`, только потом `elif i % 3 == 0` и `elif i % 5 == 0`. Если проверить 3 раньше 15, то 15 никогда не станет FizzBuzz.",
				"Число превращается в строку функцией `str(i)`. То есть `str(7)` даст `\"7\"`.",
				"Ходи по числам через `range(1, n + 1)`. При n = 0 цикл не выполнится, и вернётся пустой список - это верно."
			],
			"solution": "def fizzbuzz(n):\n    result = []\n    for i in range(1, n + 1):\n        if i % 15 == 0:\n            result.append(\"FizzBuzz\")\n        elif i % 3 == 0:\n            result.append(\"Fizz\")\n        elif i % 5 == 0:\n            result.append(\"Buzz\")\n        else:\n            result.append(str(i))\n    return result",
			"tests": [
				{
					"name": "fizzbuzz(5) == ['1','2','Fizz','4','Buzz']",
					"code": "assert fizzbuzz(5) == [\"1\", \"2\", \"Fizz\", \"4\", \"Buzz\"], \"fizzbuzz(5)\""
				},
				{
					"name": "fizzbuzz(15) целиком верный",
					"code": "assert fizzbuzz(15) == [\"1\",\"2\",\"Fizz\",\"4\",\"Buzz\",\"Fizz\",\"7\",\"8\",\"Fizz\",\"Buzz\",\"11\",\"Fizz\",\"13\",\"14\",\"FizzBuzz\"], \"fizzbuzz(15)\""
				},
				{
					"name": "15-й элемент - FizzBuzz",
					"code": "assert fizzbuzz(15)[-1] == \"FizzBuzz\", \"кратное 15 -> FizzBuzz\""
				},
				{
					"name": "fizzbuzz(1) == ['1']",
					"code": "assert fizzbuzz(1) == [\"1\"], \"fizzbuzz(1)\""
				},
				{
					"name": "fizzbuzz(0) == []",
					"code": "assert fizzbuzz(0) == [], \"n=0 -> пустой список\""
				},
				{
					"name": "все элементы - строки",
					"code": "assert all(isinstance(x, str) for x in fizzbuzz(20)), \"каждый элемент должен быть строкой\""
				}
			]
		},
		{
			"id": "is-valid-password",
			"lang": "python",
			"prompt": "Сайт требует надёжный пароль. Напиши функцию `is_valid_password(p)`, которая возвращает `True`, если пароль подходит, и `False` иначе.\n\nПароль подходит, когда **выполнены оба условия**:\n\n1. длина не меньше 6 символов;\n2. есть **хотя бы одна цифра**.\n\nНапример, `is_valid_password(\"abc123\")` равно `True`, а `is_valid_password(\"abcdefg\")` равно `False` (нет цифры).",
			"starter": "def is_valid_password(p):\n    # сначала проверь длину: если len(p) < 6, сразу верни False\n    # заведи флаг has_digit = False\n    # пройди циклом for по символам пароля\n    # если символ - цифра (метод .isdigit()), поставь has_digit = True (можно break)\n    # верни has_digit\n    pass",
			"hints": [
				"Проверить символ на цифру помогает метод: `ch.isdigit()` даёт True для '0'..'9'. Перебирай символы прямо: `for ch in p:`.",
				"Заведи флаг `has_digit = False` перед циклом. Как только нашёл цифру - ставь `has_digit = True`, дальше можно выйти через `break`.",
				"Оба условия обязательны. Удобно сначала отсеять короткие пароли: `if len(p) < 6: return False`, а в конце вернуть `has_digit`."
			],
			"solution": "def is_valid_password(p):\n    if len(p) < 6:\n        return False\n    has_digit = False\n    for ch in p:\n        if ch.isdigit():\n            has_digit = True\n            break\n    return has_digit",
			"tests": [
				{
					"name": "'abc123' -> True",
					"code": "assert is_valid_password(\"abc123\") == True, \"6 символов + цифры\""
				},
				{
					"name": "'abcde1' -> True",
					"code": "assert is_valid_password(\"abcde1\") == True, \"ровно 6 символов и есть цифра\""
				},
				{
					"name": "'abc12' -> False (короткий)",
					"code": "assert is_valid_password(\"abc12\") == False, \"длина 5 - мало\""
				},
				{
					"name": "'abcdefg' -> False (нет цифры)",
					"code": "assert is_valid_password(\"abcdefg\") == False, \"нет ни одной цифры\""
				},
				{
					"name": "'123456' -> True",
					"code": "assert is_valid_password(\"123456\") == True, \"6 цифр - подходит\""
				},
				{
					"name": "'' -> False",
					"code": "assert is_valid_password(\"\") == False, \"пустой пароль\""
				}
			]
		},
		{
			"id": "sum-even",
			"lang": "python",
			"prompt": "Считаем деньги! Напиши функцию `sum_even(n)`, которая возвращает **сумму всех чётных чисел от 1 до n включительно**.\n\nНапример, `sum_even(10)` равно `2 + 4 + 6 + 8 + 10 = 30`.\n\nЕсли чётных чисел в диапазоне нет (например, `n` равно 1), верни 0.",
			"starter": "def sum_even(n):\n    # заведи копилку total = 0\n    # пройди циклом for по числам от 1 до n включительно\n    # если число чётное (i % 2 == 0), прибавь его к копилке\n    # верни total\n    pass",
			"hints": [
				"Числа от 1 до n включительно даёт `range(1, n + 1)`.",
				"Чётность проверяется остатком: условие `if i % 2 == 0:` истинно для чётных чисел. Только тогда делай `total += i`.",
				"При n = 1 внутрь `if` мы ни разу не попадём, и вернётся 0. Не забудь `return total` после цикла."
			],
			"solution": "def sum_even(n):\n    total = 0\n    for i in range(1, n + 1):\n        if i % 2 == 0:\n            total += i\n    return total",
			"tests": [
				{
					"name": "sum_even(10) == 30",
					"code": "assert sum_even(10) == 30, \"2+4+6+8+10\""
				},
				{
					"name": "sum_even(5) == 6",
					"code": "assert sum_even(5) == 6, \"2+4\""
				},
				{
					"name": "sum_even(1) == 0",
					"code": "assert sum_even(1) == 0, \"нет чётных\""
				},
				{
					"name": "sum_even(2) == 2",
					"code": "assert sum_even(2) == 2, \"только 2\""
				},
				{
					"name": "sum_even(0) == 0",
					"code": "assert sum_even(0) == 0, \"пустой диапазон\""
				},
				{
					"name": "sum_even(100) == 2550",
					"code": "assert sum_even(100) == 2550, \"сумма чётных до 100\""
				}
			]
		},
		{
			"id": "grade-letter",
			"lang": "python",
			"prompt": "Ставим оценки! Напиши функцию `grade_letter(score)`, которая по баллам (число от 0 до 100) возвращает **буквенную оценку**:\n\n- 90 и выше -> `\"A\"`\n- 80..89 -> `\"B\"`\n- 70..79 -> `\"C\"`\n- 60..69 -> `\"D\"`\n- ниже 60 -> `\"F\"`\n\nНапример, `grade_letter(85)` равно `\"B\"`, а `grade_letter(59)` равно `\"F\"`.",
			"starter": "def grade_letter(score):\n    # используй цепочку if / elif / else\n    # ВАЖНО: проверяй границы сверху вниз, от большего к меньшему\n    # верни нужную букву как строку\n    pass",
			"hints": [
				"Идём от большего порога к меньшему: сначала `if score >= 90:`, потом `elif score >= 80:` и так далее.",
				"Порядок критичен! Если начать с `score >= 60`, то оценка 95 тоже попадёт в эту ветку и вернёт неверную букву.",
				"Последний случай (ниже 60) удобно закрыть через `else: return \"F\"` - отдельная проверка не нужна."
			],
			"solution": "def grade_letter(score):\n    if score >= 90:\n        return \"A\"\n    elif score >= 80:\n        return \"B\"\n    elif score >= 70:\n        return \"C\"\n    elif score >= 60:\n        return \"D\"\n    else:\n        return \"F\"",
			"tests": [
				{
					"name": "grade_letter(95) == 'A'",
					"code": "assert grade_letter(95) == \"A\", \"95 -> A\""
				},
				{
					"name": "grade_letter(90) == 'A' (граница)",
					"code": "assert grade_letter(90) == \"A\", \"ровно 90 -> A\""
				},
				{
					"name": "grade_letter(85) == 'B'",
					"code": "assert grade_letter(85) == \"B\", \"85 -> B\""
				},
				{
					"name": "grade_letter(72) == 'C'",
					"code": "assert grade_letter(72) == \"C\", \"72 -> C\""
				},
				{
					"name": "grade_letter(60) == 'D' (граница)",
					"code": "assert grade_letter(60) == \"D\", \"ровно 60 -> D\""
				},
				{
					"name": "grade_letter(59) == 'F'",
					"code": "assert grade_letter(59) == \"F\", \"59 -> F\""
				},
				{
					"name": "grade_letter(0) == 'F'",
					"code": "assert grade_letter(0) == \"F\", \"0 -> F\""
				}
			]
		},
		{
			"id": "first-cold-day",
			"lang": "python",
			"prompt": "Собираемся в путешествие и смотрим прогноз погоды. Напиши функцию `first_cold_day(temps)`, которая принимает список температур по дням и возвращает **номер (индекс) первого дня**, когда температура **ниже нуля**.\n\nЕсли холодных дней нет, верни `-1`.\n\nНапример, `first_cold_day([5, 3, -1, 8])` равно `2` (индексы считаем с нуля, первый минус - на третьем дне). А `first_cold_day([10, 20, 30])` равно `-1`.\n\nЗдесь удобно применить цикл `while` и `break` (или `return` при находке).",
			"starter": "def first_cold_day(temps):\n    # заведи индекс day = 0\n    # пока day меньше длины списка (while day < len(temps)):\n    #   если temps[day] < 0, сразу верни day\n    #   иначе увеличь day на 1\n    # если ничего не нашли - верни -1\n    pass",
			"hints": [
				"Начни с `day = 0` и цикла `while day < len(temps):`. Обращайся к элементу по индексу: `temps[day]`.",
				"Как только `temps[day] < 0` - можно сразу `return day`, это и есть искомый первый холодный день.",
				"Не забывай увеличивать индекс `day += 1` внутри цикла, иначе будет бесконечный цикл. Если цикл закончился без находки - `return -1`. Ноль не считается \"ниже нуля\"!"
			],
			"solution": "def first_cold_day(temps):\n    day = 0\n    while day < len(temps):\n        if temps[day] < 0:\n            return day\n        day += 1\n    return -1",
			"tests": [
				{
					"name": "first_cold_day([5,3,-1,8]) == 2",
					"code": "assert first_cold_day([5, 3, -1, 8]) == 2, \"первый минус на индексе 2\""
				},
				{
					"name": "нет холодных дней -> -1",
					"code": "assert first_cold_day([10, 20, 30]) == -1, \"все дни тёплые\""
				},
				{
					"name": "первый день холодный -> 0",
					"code": "assert first_cold_day([-5, 2, 3]) == 0, \"минус в самом начале\""
				},
				{
					"name": "пустой список -> -1",
					"code": "assert first_cold_day([]) == -1, \"нет данных\""
				},
				{
					"name": "ноль не считается холодным",
					"code": "assert first_cold_day([0, 0, 0]) == -1, \"0 не ниже нуля\""
				},
				{
					"name": "first_cold_day([1,0,-2]) == 2",
					"code": "assert first_cold_day([1, 0, -2]) == 2, \"0 пропускаем, -2 подходит\""
				}
			]
		}
	],
	"quiz": [
		{
			"question": "Что вернёт `grade_letter(95)`, если проверки в функции идут в неправильном порядке - сначала `if score >= 60: return \"D\"`, а потом уже `elif score >= 90: return \"A\"`?",
			"options": [
				"\"A\"",
				"\"D\"",
				"\"F\"",
				"Ошибку"
			],
			"correct": 1,
			"explain": "Ветки if/elif проверяются сверху вниз, и срабатывает ПЕРВАЯ подходящая. 95 >= 60 истинно, поэтому вернётся \"D\", а до проверки на 90 дело не дойдёт. Поэтому пороги ставят от большего к меньшему."
		},
		{
			"question": "В цикле `for` при обработке числа сработал `continue`. Что произойдёт?",
			"options": [
				"Цикл полностью остановится",
				"Программа завершится с ошибкой",
				"Остаток текущего шага пропустится, и цикл перейдёт к следующему числу",
				"Цикл начнётся заново с первого элемента"
			],
			"correct": 2,
			"explain": "`continue` пропускает оставшийся код внутри текущего шага и сразу переходит к следующей итерации. А вот `break` - тот полностью выходит из цикла."
		},
		{
			"question": "Почему в проверке пароля важно, что нужны ОБА условия - и длина >= 6, и наличие цифры?",
			"options": [
				"Достаточно любого одного из них",
				"Если убрать одно условие, слабый пароль (например, короткий или без цифры) ошибочно пройдёт проверку",
				"Цифра важнее длины, длину можно не проверять",
				"Условия не связаны и проверяются независимо от результата"
			],
			"correct": 1,
			"explain": "Оба условия обязательны одновременно. Уберём проверку длины - пройдёт короткий \"a1\". Уберём проверку цифры - пройдёт \"abcdef\" без цифр. Надёжность даёт именно сочетание обоих правил."
		}
	]
};
