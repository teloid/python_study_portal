/** @type {import('../schema').Lesson} */
export default {
	"slug": "lists-collections",
	"title": "Списки: коллекции данных в одном месте",
	"level": "beginner",
	"topic": "python",
	"order": 8,
	"estimatedMinutes": 55,
	"emoji": "📋",
	"summary": "Списки позволяют хранить много значений в одной переменной. Учимся создавать списки, добавлять и удалять элементы, обращаться по индексу, делать срезы и проходить циклом.",
	"goals": [
		"Создавать списки и обращаться к элементам по индексу",
		"Добавлять и удалять элементы (.append(), .remove())",
		"Узнавать длину через len() и делать срезы",
		"Проходить по списку циклом for",
		"Сортировать список .sort() и sorted()"
	],
	"theory": [
		{
			"type": "text",
			"md": "Представь, что у тебя есть много данных, которые хочется хранить вместе: список покупок, имена друзей, оценки за контрольные. Заводить отдельную переменную под каждое значение неудобно. Для этого в Python есть **список** (`list`) - одна коробка, в которую можно сложить сколько угодно элементов и держать их по порядку."
		},
		{
			"type": "text",
			"md": "Список создаётся при помощи квадратных скобок `[]`. Элементы внутри разделяются запятыми. В одном списке могут быть строки, числа или что угодно ещё:\n\n```\nfruits = [\"яблоко\", \"банан\", \"вишня\"]\nnumbers = [10, 20, 30]\nempty = []  # пустой список\n```"
		},
		{
			"type": "text",
			"md": "Каждый элемент списка имеет свой номер - **индекс**. Важно: счёт начинается с **нуля**, а не с единицы. То есть первый элемент имеет индекс `0`, второй - `1` и так далее. А ещё можно считать с конца: индекс `-1` это последний элемент."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "fruits = [\"яблоко\", \"банан\", \"вишня\"]\nprint(fruits[0])   # первый\nprint(fruits[2])   # третий\nprint(fruits[-1])  # последний",
			"runnable": true,
			"output": "яблоко\nвишня\nвишня"
		},
		{
			"type": "callout",
			"variant": "warning",
			"title": "Не выходи за границы",
			"md": "Если в списке 3 элемента, то индексы у них только `0`, `1` и `2`. Если попросить `fruits[3]`, Python выдаст ошибку `IndexError`. Такого элемента просто нет."
		},
		{
			"type": "text",
			"md": "Можно взять не один элемент, а целый кусок списка - это называется **срез**. Пишем два индекса через двоеточие: `список[начало:конец]`. Элемент с индексом `конец` НЕ включается. А функция `len` подскажет, сколько всего элементов в списке."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "numbers = [10, 20, 30, 40, 50]\nprint(numbers[1:3])  # элементы с индексами 1 и 2\nprint(len(numbers))  # сколько всего элементов",
			"runnable": true,
			"output": "[20, 30]\n5"
		},
		{
			"type": "text",
			"md": "Списки можно менять прямо на ходу. Два самых частых действия:\n\n- `.append()` - добавляет новый элемент в **конец** списка\n- `.remove()` - удаляет первый найденный элемент с указанным значением"
		},
		{
			"type": "code",
			"lang": "python",
			"code": "colors = [\"красный\", \"зелёный\"]\ncolors.append(\"синий\")\nprint(colors)\ncolors.remove(\"зелёный\")\nprint(colors)",
			"runnable": true,
			"output": "['красный', 'зелёный', 'синий']\n['красный', 'синий']"
		},
		{
			"type": "text",
			"md": "Очень часто нужно пройтись по всем элементам списка по очереди. Для этого идеально подходит цикл `for`. На каждом шаге переменная (тут `fruit`) принимает значение очередного элемента."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "for fruit in [\"яблоко\", \"банан\"]:\n    print(\"Люблю\", fruit)",
			"runnable": true,
			"output": "Люблю яблоко\nЛюблю банан"
		},
		{
			"type": "text",
			"md": "Часто список хочется упорядочить. Тут есть два способа, и важно понимать разницу между ними:\n\n- `sorted()` - функция, которая **возвращает новый** отсортированный список, а исходный оставляет нетронутым.\n- `.sort()` - метод, который меняет **сам** список на месте и ничего не возвращает (вернёт `None`)."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "nums = [3, 1, 2]\nprint(sorted(nums))  # новый список\nprint(nums)          # старый не изменился\nnums.sort()          # меняем сам список\nprint(nums)",
			"runnable": true,
			"output": "[1, 2, 3]\n[3, 1, 2]\n[1, 2, 3]"
		},
		{
			"type": "text",
			"md": "И последнее на сегодня: проверить, есть ли элемент в списке, можно ключевым словом `in`. Оно даёт `True`, если элемент найден, и `False`, если нет. Очень удобно для проверок."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "print(\"банан\" in [\"яблоко\", \"банан\"])\nprint(\"груша\" in [\"яблоко\", \"банан\"])",
			"runnable": true,
			"output": "True\nFalse"
		},
		{
			"type": "callout",
			"variant": "tip",
			"title": "Коротко о главном",
			"md": "- Список это упорядоченная коллекция в квадратных скобках `[]`.\n- Доступ по индексу с `0`, последний элемент это `-1`.\n- `.append()` добавляет, `.remove()` удаляет, `len` считает.\n- `for` проходит по всем элементам, `in` проверяет наличие.\n- `sorted` создаёт новый список, `.sort()` меняет старый."
		}
	],
	"exercises": [
		{
			"id": "add-city",
			"lang": "python",
			"prompt": "Напиши функцию `add_city(cities, city)`, которая добавляет город `city` в **конец** списка `cities` и возвращает этот же список.\n\nНапример, `add_city([\"Москва\", \"Казань\"], \"Сочи\")` должно вернуть `[\"Москва\", \"Казань\", \"Сочи\"]`.",
			"starter": "def add_city(cities, city):\n    # добавь city в конец списка cities методом .append()\n    # затем верни список cities\n    pass",
			"hints": [
				"Чтобы добавить элемент в конец списка, используй метод `.append()`: `cities.append(city)`.",
				"После добавления нужно вернуть сам список: `return cities`.",
				"Не создавай новый список - работай с тем, что пришёл в аргументе `cities`."
			],
			"solution": "def add_city(cities, city):\n    cities.append(city)\n    return cities",
			"tests": [
				{
					"name": "add_city([\"Москва\", \"Казань\"], \"Сочи\")",
					"code": "assert add_city([\"Москва\", \"Казань\"], \"Сочи\") == [\"Москва\", \"Казань\", \"Сочи\"], \"должен добавиться Сочи в конец\""
				},
				{
					"name": "добавление в пустой список",
					"code": "assert add_city([], \"Пермь\") == [\"Пермь\"], \"в пустой список добавляется один элемент\""
				},
				{
					"name": "длина и последний элемент",
					"code": "result = add_city([\"Омск\"], \"Тверь\")\nassert len(result) == 2, \"после добавления длина должна стать 2\"\nassert result[-1] == \"Тверь\", \"последний элемент должен быть Тверь\""
				}
			]
		},
		{
			"id": "sort-scores",
			"lang": "python",
			"prompt": "Напиши функцию `sort_scores(scores)`, которая принимает список чисел и возвращает **новый** список, отсортированный по возрастанию. Исходный список менять нельзя.\n\nНапример, `sort_scores([5, 2, 9, 1])` должно вернуть `[1, 2, 5, 9]`.",
			"starter": "def sort_scores(scores):\n    # верни новый отсортированный список с помощью sorted()\n    pass",
			"hints": [
				"Функция `sorted()` возвращает новый отсортированный список и не трогает оригинал - то, что нужно.",
				"Просто верни результат: `return sorted(scores)`.",
				"Не используй метод `.sort()` - он изменил бы исходный список, а по условию его трогать нельзя."
			],
			"solution": "def sort_scores(scores):\n    return sorted(scores)",
			"tests": [
				{
					"name": "sort_scores([5, 2, 9, 1])",
					"code": "assert sort_scores([5, 2, 9, 1]) == [1, 2, 5, 9], \"числа должны идти по возрастанию\""
				},
				{
					"name": "исходный список не меняется",
					"code": "original = [3, 1, 2]\nsort_scores(original)\nassert original == [3, 1, 2], \"исходный список не должен меняться\""
				},
				{
					"name": "пустой список и один элемент",
					"code": "assert sort_scores([]) == [], \"пустой список остаётся пустым\"\nassert sort_scores([7]) == [7], \"список из одного элемента не меняется\""
				}
			]
		},
		{
			"id": "find-max",
			"lang": "python",
			"prompt": "Напиши функцию `find_max(numbers)`, которая находит и возвращает наибольшее число в списке `numbers`. Сделай это **без** встроенной функции `max` - пройди по списку циклом `for` сам.\n\nНапример, `find_max([3, 8, 1, 5])` должно вернуть `8`. Можно считать, что список не пустой.",
			"starter": "def find_max(numbers):\n    # 1. заведи переменную и положи в неё первый элемент numbers[0]\n    # 2. пройди циклом for по всем числам\n    # 3. если число больше текущего максимума - запомни его\n    # 4. верни максимум\n    pass",
			"hints": [
				"Начни с того, что считаешь максимумом первый элемент: `biggest = numbers[0]`.",
				"В цикле `for n in numbers:` сравнивай каждое `n` с `biggest` через `if n > biggest`.",
				"Если нашёл число больше, обнови переменную: `biggest = n`. В конце не забудь `return biggest`."
			],
			"solution": "def find_max(numbers):\n    biggest = numbers[0]\n    for n in numbers:\n        if n > biggest:\n            biggest = n\n    return biggest",
			"tests": [
				{
					"name": "find_max([3, 8, 1, 5])",
					"code": "assert find_max([3, 8, 1, 5]) == 8, \"максимум из [3, 8, 1, 5] это 8\""
				},
				{
					"name": "отрицательные числа",
					"code": "assert find_max([-5, -2, -10]) == -2, \"максимум среди отрицательных это -2\""
				},
				{
					"name": "один элемент и одинаковые",
					"code": "assert find_max([42]) == 42, \"один элемент это и есть максимум\"\nassert find_max([4, 4, 4]) == 4, \"если все одинаковые, максимум равен им\""
				}
			]
		}
	],
	"quiz": [
		{
			"question": "Какой индекс у первого элемента списка в Python?",
			"options": [
				"1",
				"0",
				"-1"
			],
			"correct": 1,
			"explain": "В Python нумерация элементов начинается с нуля, поэтому первый элемент имеет индекс 0. Индекс -1 указывает на последний элемент."
		},
		{
			"question": "Чем отличается sorted(nums) от nums.sort()?",
			"options": [
				"Ничем, это одно и то же",
				"sorted() возвращает новый отсортированный список и не меняет исходный, а .sort() сортирует сам список на месте",
				".sort() возвращает новый список, а sorted() ничего не возвращает"
			],
			"correct": 1,
			"explain": "Функция sorted() создаёт и возвращает новый отсортированный список, оставляя оригинал нетронутым. Метод .sort() меняет сам список на месте и возвращает None."
		},
		{
			"question": "Что делает метод .append() у списка?",
			"options": [
				"Добавляет элемент в конец списка",
				"Удаляет последний элемент",
				"Сортирует список по возрастанию"
			],
			"correct": 0,
			"explain": "Метод .append() добавляет переданный элемент в конец списка. Для удаления используется .remove(), а для сортировки .sort() или sorted()."
		}
	]
};
