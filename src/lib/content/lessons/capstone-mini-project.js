/** @type {import('../schema').Lesson} */
export default {
	"slug": "capstone-mini-project",
	"title": "Финальный проект: собираем всё вместе",
	"level": "intermediate",
	"topic": "python",
	"order": 15,
	"estimatedMinutes": 90,
	"emoji": "🏆",
	"summary": "Мягкий капстон, объединяющий все навыки курса. Строим небольшое консольное приложение, которое использует ввод, условия, циклы, списки, словари, функции и обработку ошибок.",
	"goals": [
		"Спланировать программу из нескольких функций",
		"Объединить ввод, условия, циклы, списки и словари",
		"Применить обработку ошибок для устойчивости",
		"Познакомиться с простым классом",
		"Довести небольшой проект до рабочего состояния"
	],
	"theory": [
		{
			"type": "text",
			"md": "Поздравляю, ты дошёл до финального урока! Это **капстон** - проект, в котором мы соберём вместе всё, что уже знаем: функции `def`, списки `list`, словари `dict`, обработку ошибок `try` и `except`, а ещё познакомимся с классами `class`. Не волнуйся, ничего нового и страшного не будет - мы просто красиво сложим знакомые кубики в один маленький проект."
		},
		{
			"type": "text",
			"md": "Наш мини-проект - **менеджер задач** (список дел). Он умеет:\n- хранить список задач,\n- добавлять новую задачу,\n- отмечать задачу выполненной,\n- показывать всё на экране.\n\nГлавный приём, который мы используем: каждую отдельную операцию делаем **функцией**. Так код легко читать, проверять и переиспользовать."
		},
		{
			"type": "text",
			"md": "Сначала вспомним, как удобно хранить одну задачу. Возьмём словарь `dict`: у него есть ключи и значения. Для задачи нам хватит двух ключей - `title` (название) и `done` (сделана ли, `True` или `False`)."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "tasks = []\ntasks.append({\"title\": \"Учить Python\", \"done\": False})\ntasks.append({\"title\": \"Сделать зарядку\", \"done\": True})\nfor task in tasks:\n    znak = \"[x]\" if task[\"done\"] else \"[ ]\"\n    print(znak, task[\"title\"])",
			"runnable": true,
			"output": "[ ] Учить Python\n[x] Сделать зарядку"
		},
		{
			"type": "text",
			"md": "Здесь `tasks` - это список `list` из словарей. Мы добавляем задачи через `.append()`, а потом проходим по ним циклом `for` и печатаем. Это уже почти настоящее приложение!"
		},
		{
			"type": "callout",
			"variant": "tip",
			"title": "Главная идея капстона",
			"md": "Большую программу всегда удобнее собирать из маленьких функций. Каждая функция делает **одно** понятное дело и возвращает результат через `return`. Тогда программу легко проверять по частям."
		},
		{
			"type": "text",
			"md": "Теперь лёгкое знакомство с `class`. Класс - это просто способ хранить **данные и поведение вместе**. Представь шаблон (форму для печенья): по нему можно сделать много одинаковых объектов. Внутри класса данные лежат в `self`, а функции внутри класса называются **методами**."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "class Task:\n    def __init__(self, title):\n        self.title = title\n        self.done = False\n\n    def mark_done(self):\n        self.done = True\n\nt = Task(\"Полить цветы\")\nprint(t.title)\nprint(t.done)\nt.mark_done()\nprint(t.done)",
			"runnable": true,
			"output": "Полить цветы\nFalse\nTrue"
		},
		{
			"type": "text",
			"md": "Разберём по шагам:\n- `class Task:` - создаём шаблон с именем `Task`.\n- `__init__` - особый метод, он срабатывает при создании объекта и кладёт начальные данные в `self`.\n- `self.title` и `self.done` - это данные конкретной задачи.\n- `mark_done` - метод (поведение): меняет `done` на `True`.\n\nОбъект `t = Task(\"Полить цветы\")` - это одна конкретная задача, сделанная по шаблону. На этом уроке достаточно понимать класс именно так: данные плюс действия в одной коробочке. В упражнениях мы будем работать со словарями - так проще, но теперь ты узнаёшь класс в чужом коде."
		},
		{
			"type": "callout",
			"variant": "info",
			"title": "Словарь или класс?",
			"md": "Для маленьких задач словарь `dict` отлично подходит и его быстрее писать. Класс `class` пригодится, когда у объекта много данных и поведения. Оба подхода правильные - выбирай тот, что проще для конкретной задачи."
		},
		{
			"type": "text",
			"md": "Последний кубик - надёжность. В реальной программе данные бывают кривыми: вместо числа вдруг пришёл текст. Чтобы программа не падала, оборачиваем рискованное место в `try` и `except`. Это как страховка: пробуем сделать, а если не вышло - спокойно обрабатываем ошибку."
		},
		{
			"type": "code",
			"lang": "python",
			"code": "expenses = {\"кофе\": 150, \"такси\": 300}\nprint(sum(expenses.values()))\n\ntry:\n    x = int(\"не число\")\nexcept ValueError:\n    print(\"Не получилось превратить в число\")",
			"runnable": true,
			"output": "450\nНе получилось превратить в число"
		},
		{
			"type": "text",
			"md": "Здесь мы заодно увидели вторую идею проекта - **учёт расходов**. Расходы удобно хранить в словаре `dict`: ключ - на что потратили, значение - сколько. А функция `sum` вместе с `.values()` мгновенно считает общую сумму. В упражнениях ты сам напишешь такие функции."
		},
		{
			"type": "callout",
			"variant": "warning",
			"title": "Не лови всё подряд",
			"md": "Старайся ловить конкретные ошибки: `except ValueError`, `except TypeError`. Если написать просто `except:` без типа, то можно случайно спрятать настоящую проблему в коде и потом долго её искать."
		},
		{
			"type": "text",
			"md": "Итак, рецепт любого мини-проекта:\n- продумай, **что хранить** (список `list`, словарь `dict` или класс `class`),\n- раздели работу на **маленькие функции** `def`,\n- каждую функцию заставь **возвращать** результат через `return`,\n- защити рискованные места через `try` и `except`.\n\nДальше - три упражнения. Каждое - это кусочек настоящего приложения. Собери их, и у тебя будет рабочая основа менеджера задач и трекера расходов. Ты уже умеешь больше, чем думаешь. Вперёд!"
		}
	],
	"exercises": [
		{
			"id": "add-task",
			"lang": "python",
			"prompt": "Напиши функцию `add_task(tasks, title)` для менеджера задач.\n\nОна принимает список задач `tasks` и название новой задачи `title`, а возвращает **новый** список, в конце которого добавлена задача-словарь вида `{\"title\": title, \"done\": False}`.\n\n**Важно:** исходный список `tasks` менять нельзя - верни именно новый список (подсказка: складывать списки можно через `+`).",
			"starter": "def add_task(tasks, title):\n    # 1. Создай словарь новой задачи: ключ \"title\" = title, ключ \"done\" = False\n    # 2. Верни новый список: старый список плюс список с новой задачей\n    pass",
			"hints": [
				"Новая задача - это словарь: `{\"title\": title, \"done\": False}`.",
				"Чтобы не менять исходный список, не используй `.append()`. Сложи два списка через `+`.",
				"Заверни новую задачу в список из одного элемента: `tasks + [new_task]`."
			],
			"solution": "def add_task(tasks, title):\n    new_task = {\"title\": title, \"done\": False}\n    return tasks + [new_task]",
			"tests": [
				{
					"name": "Добавление в пустой список",
					"code": "assert add_task([], \"Купить хлеб\") == [{\"title\": \"Купить хлеб\", \"done\": False}], \"в пустой список должна добавиться одна задача\""
				},
				{
					"name": "Новая задача не сделана",
					"code": "assert add_task([], \"X\")[0][\"done\"] == False, \"у новой задачи done должно быть False\""
				},
				{
					"name": "Исходный список не меняется",
					"code": "base = [{\"title\": \"Старая\", \"done\": True}]\nadd_task(base, \"Новая\")\nassert base == [{\"title\": \"Старая\", \"done\": True}], \"исходный список менять нельзя\""
				},
				{
					"name": "Задача добавляется в конец",
					"code": "base = [{\"title\": \"Старая\", \"done\": True}]\nr = add_task(base, \"Новая\")\nassert len(r) == 2 and r[1] == {\"title\": \"Новая\", \"done\": False}, \"новая задача должна быть последней\""
				}
			]
		},
		{
			"id": "total-expenses",
			"lang": "python",
			"prompt": "Теперь трекер расходов. Напиши функцию `total(expenses)`.\n\nОна принимает словарь `dict`, где ключ - название покупки, а значение - её цена (число). Функция должна вернуть **сумму всех значений**.\n\nДля пустого словаря результат - `0`.",
			"starter": "def total(expenses):\n    # Заведи переменную-накопитель, например summa = 0\n    # Пройди циклом for по значениям словаря (.values()) и прибавляй их\n    # Верни summa\n    pass",
			"hints": [
				"Получить все значения словаря можно методом `.values()`.",
				"Заведи переменную `summa = 0` и в цикле `for` прибавляй к ней каждое значение.",
				"Можно даже короче - встроенная функция `sum` умеет складывать: `return sum(expenses.values())`."
			],
			"solution": "def total(expenses):\n    summa = 0\n    for value in expenses.values():\n        summa = summa + value\n    return summa",
			"tests": [
				{
					"name": "Пустой словарь даёт 0",
					"code": "assert total({}) == 0, \"для пустого словаря сумма должна быть 0\""
				},
				{
					"name": "Одна покупка",
					"code": "assert total({\"кофе\": 150}) == 150, \"сумма одной покупки равна её цене\""
				},
				{
					"name": "Несколько покупок",
					"code": "assert total({\"кофе\": 150, \"такси\": 300, \"обед\": 250}) == 700, \"должна получиться сумма всех значений\""
				},
				{
					"name": "Ещё одна проверка суммы",
					"code": "assert total({\"a\": 10, \"b\": 20, \"c\": 5}) == 35, \"сумма 10+20+5 должна быть 35\""
				}
			]
		},
		{
			"id": "most-expensive",
			"lang": "python",
			"prompt": "Финальная функция - `most_expensive(expenses)`.\n\nОна принимает словарь расходов (ключ - название, значение - цена) и возвращает **название самой дорогой покупки**.\n\nДополнительно сделай функцию надёжной:\n- если словарь пустой - верни `None`;\n- если на вход пришёл вообще не словарь (например число или строка), функция не должна падать - тоже верни `None`. Используй `try` и `except`.",
			"starter": "def most_expensive(expenses):\n    # Оберни перебор в try/except, чтобы не словарь не ломал программу\n    # Иди по expenses.items() и запоминай покупку с самой большой ценой\n    # Если ничего не нашлось (пусто) - верни None\n    pass",
			"hints": [
				"Перебирай пары через `for name, value in expenses.items()`. Заведи best_name и best_value со значением None.",
				"Если best_value ещё None или текущее value больше best_value - обнови best_name и best_value.",
				"Оберни всё в `try` и поймай `except (AttributeError, TypeError)` - тогда не словарь вернёт None вместо ошибки."
			],
			"solution": "def most_expensive(expenses):\n    try:\n        best_name = None\n        best_value = None\n        for name, value in expenses.items():\n            if best_value is None or value > best_value:\n                best_value = value\n                best_name = name\n        return best_name\n    except (AttributeError, TypeError):\n        return None",
			"tests": [
				{
					"name": "Самая дорогая из нескольких",
					"code": "assert most_expensive({\"кофе\": 150, \"такси\": 300, \"обед\": 250}) == \"такси\", \"самая дорогая покупка - такси\""
				},
				{
					"name": "Одна покупка",
					"code": "assert most_expensive({\"книга\": 500}) == \"книга\", \"при одной покупке возвращается она же\""
				},
				{
					"name": "Пустой словарь даёт None",
					"code": "assert most_expensive({}) is None, \"для пустого словаря результат None\""
				},
				{
					"name": "Строка вместо словаря даёт None",
					"code": "assert most_expensive(\"не словарь\") is None, \"строка не должна ломать функцию\""
				},
				{
					"name": "Число вместо словаря даёт None",
					"code": "assert most_expensive(42) is None, \"число не должно ломать функцию\""
				}
			]
		}
	],
	"quiz": [
		{
			"question": "Что такое метод `__init__` в классе?",
			"options": [
				"Особый метод, который автоматически вызывается при создании объекта и задаёт его начальные данные",
				"Функция, которая удаляет объект из памяти",
				"Обычная переменная внутри класса",
				"Команда, которая печатает объект на экране"
			],
			"correct": 0,
			"explain": "`__init__` срабатывает в момент создания объекта и кладёт начальные данные в `self`. Например, в нём мы задаём `self.title` и `self.done`."
		},
		{
			"question": "Зачем рискованный код оборачивают в `try` и `except`?",
			"options": [
				"Чтобы код выполнялся быстрее",
				"Чтобы при ошибке программа не падала, а спокойно обработала проблему",
				"Чтобы спрятать код от других программистов",
				"Чтобы цикл `for` повторялся несколько раз"
			],
			"correct": 1,
			"explain": "`try`/`except` - это страховка: мы пробуем выполнить рискованную операцию, а если возникает ошибка, ловим её в `except` и обрабатываем, вместо того чтобы программа аварийно завершилась."
		},
		{
			"question": "В функции `add_task` мы вернули `tasks + [new_task]` вместо `tasks.append(new_task)`. Почему это важно?",
			"options": [
				"Потому что `.append()` работает только со словарями",
				"Потому что сложение списков работает быстрее в любой ситуации",
				"Потому что так мы возвращаем новый список и не изменяем исходный",
				"Потому что `+` автоматически сортирует список"
			],
			"correct": 2,
			"explain": "По условию исходный список менять нельзя. `.append()` изменил бы старый список на месте, а `tasks + [new_task]` создаёт новый список, оставляя оригинал нетронутым."
		}
	]
};
